# fileObject.seek(offset[,whence])
# offset，开始的偏移量，代表需要移动偏移的字节数
# whence，可选，默认值为0，给offset参数一个定义，表示要从那个位置开始偏移
# 0：代表从文件开头开始算起，1：代表从当前位置开始算起，2：代表从文件末尾算起

# fileObject.seek(0,0) 返回文件开头

# fileObject.tell() 返回当前指针的位置
